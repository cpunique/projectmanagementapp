rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ============================================
    // Helper Functions
    // ============================================

    // Check if user is authenticated
    function isAuthenticated() {
      return request.auth != null;
    }

    // Check if user is the owner (for read/update operations)
    function isOwner(boardId) {
      return isAuthenticated() && request.auth.uid == resource.data.ownerId;
    }

    // Validate board data structure and constraints
    function isValidBoardData() {
      let data = request.resource.data;
      return data.keys().hasAll(['id', 'name', 'columns', 'ownerId', 'sharedWith']) &&
             data.name is string &&
             data.name.size() > 0 &&
             data.name.size() <= 100 &&
             data.columns is list &&
             data.columns.size() <= 50 &&
             data.sharedWith is list &&
             data.ownerId is string;
    }

    // Check document size is reasonable (< 1MB)
    function isValidBoardSize() {
      return request.resource.size() < 1000000;
    }

    // Ensure ownerId never changes during updates
    function ownershipUnchanged() {
      return request.resource.data.ownerId == resource.data.ownerId;
    }

    // Allow deletion of 'default-board' specifically (corrupted board recovery)
    function isDefaultBoardCorrupted(boardId) {
      return boardId == 'default-board' && isAuthenticated();
    }

    // Check if user can access a board (owner or collaborator)
    function canAccessBoard(boardId) {
      let boardData = get(/databases/$(database)/documents/boards/$(boardId)).data;
      return request.auth.uid == boardData.ownerId ||
             boardData.sharedWith.hasAny([{'userId': request.auth.uid}]);
    }

    // Check if user can edit a board (owner or editor collaborator)
    function canEditBoard(boardId) {
      // Owner can always edit
      let boardData = get(/databases/$(database)/documents/boards/$(boardId)).data;
      if (request.auth.uid == boardData.ownerId) return true;

      // Check if user is an editor collaborator
      let collaborators = boardData.sharedWith;
      return collaborators.hasAny([{
        'userId': request.auth.uid,
        'role': 'editor'
      }]);
    }

    // ============================================
    // Users Collection Rules
    // ============================================

    match /users/{userId} {
      // Users can read their own document
      allow read: if isAuthenticated() && request.auth.uid == userId;

      // Users can create their own document
      allow create: if isAuthenticated() && request.auth.uid == userId;

      // Users can update their own document
      allow update: if isAuthenticated() && request.auth.uid == userId;

      // Prevent user deletion
      allow delete: if false;
    }

    // ============================================
    // Boards Collection Rules - OWNER-ONLY ACCESS
    // ============================================

    match /boards/{boardId} {

      // Read: Owner or collaborator can read
      allow read: if isAuthenticated() && canAccessBoard(boardId);

      // Create: Can create if setting self as owner and no sharing initially
      allow create: if isAuthenticated() &&
                      request.resource.data.ownerId == request.auth.uid &&
                      request.resource.data.sharedWith == [] &&
                      isValidBoardData() &&
                      isValidBoardSize();

      // Update: Owner or editor collaborator can update, and ownerId cannot change
      allow update: if isAuthenticated() &&
                      canEditBoard(boardId) &&
                      ownershipUnchanged() &&
                      isValidBoardData() &&
                      isValidBoardSize();

      // Delete: Only owner can delete, or allow special case for corrupted 'default-board'
      allow delete: if (isAuthenticated() && isOwner(boardId)) || isDefaultBoardCorrupted(boardId);

      // ============================================
      // Nested Collections (future use)
      // ============================================

      // Comments subcollection (for future feature)
      match /comments/{commentId} {
        allow read: if isAuthenticated() && canAccessBoard(boardId);
        allow create: if isAuthenticated() && canAccessBoard(boardId) &&
                       request.resource.data.userId == request.auth.uid;
        allow update: if isAuthenticated() &&
                      request.resource.data.userId == request.auth.uid;
        allow delete: if isAuthenticated() &&
                      resource.data.userId == request.auth.uid;
      }

      // Attachments subcollection (for future feature)
      match /attachments/{attachmentId} {
        allow read: if isAuthenticated() && canAccessBoard(boardId);
        allow create: if isAuthenticated() && canAccessBoard(boardId) &&
                      request.resource.data.uploadedBy == request.auth.uid;
        allow delete: if isAuthenticated() &&
                     resource.data.uploadedBy == request.auth.uid;
      }
    }

    // ============================================
    // Demo Configs Collection Rules
    // ============================================

    match /demo-configs/{configId} {
      // Anyone can read demo configs (needed for landing page to fetch demo board)
      allow read: if true;

      // Only the designated admin can write
      allow write: if isAuthenticated() &&
                      request.auth.uid == 'OWDbFDLVxgfW6ftSXoQBurAvAiB2';
    }

    // ============================================
    // Catch-All (Deny by default)
    // ============================================

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
